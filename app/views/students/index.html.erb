<h1>List of Students</h1>
<%= link_to "Add Student", new_student_path, class: "btn btn-xs btn-primary" %>
<% @students.each do |student| %>
<p><%= link_to student.name, student_path(student)%></p>
<%end%>
